{
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.3",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.16",
    "stripe": "^17.3.0",
    "type": "module"
  },
  "scripts": {
  "start": "node server.js",
  "server": "nodemon server.js",
  "build": "echo 'Building the backend...' && npm install && npm run test",
  "test": "jest",
  "test:watch": "jest --watch",
  "lint": "eslint .",
  "lint:fix": "eslint . --fix",
  "deploy": "echo 'Deploying the backend...' && npm run build"
},
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "supertest": "^7.0.0"
  }
}
